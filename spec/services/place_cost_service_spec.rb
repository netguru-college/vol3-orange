require 'rails_helper'

# RSpec.describe TripCostService, type: :service do
#   let(:place) { build(:place) }
#   subject { described_class.new(place) }
#
#   describe '#call' do
#
#     before do
#       allow(subject).to receive_message_chain(
#         :attributes, :map, :sum).with(no_args)
#         .with({ |attr| sum_attr_cost(attr) })
#         .with(no_args).and_return(result)
#     end
#
#     it { expect(subject.call).to be_truthy }
#
#   end
# end
