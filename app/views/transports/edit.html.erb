<h1> Edit transport to <%= @place %> </h1>

<%= render 'form', transport: @transport %>

<%= link_to 'Delete',
             trip_place_transport_path(@trip, @place, @transport),
                         method: :delete,
                         data: { confirm: 'Are you sure?' } %>
<%= link_to 'Back', trip_place_path(@trip, @place), class: 'btn btn-primary' %>
